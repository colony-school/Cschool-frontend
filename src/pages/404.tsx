// External modules import
import { NextPage } from "next";

// Main component
const NotFound: NextPage = () => {
    return (
        <div>
            Someone probably did an oopsie
        </div>
    );
}

// Exports
export default NotFound;